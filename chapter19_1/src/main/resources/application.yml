spring:
  rabbitmq:
    username: guest    # 默认为`guest`
    password: guest    # 默认为`guest`
    host: localhost    # 默认为`localhost`
    port: 5672  # 默认为 5672
    virtual-host: /
    # 开启自动ACK，默认为`none`（no ack）
    listener:
      simple:
        acknowledge-mode: auto